# Used by `vercel dev`. iCount creds are NOT here — they come from per-request headers.
# The only reason to set anything here is to gate the public URL.

# Optional: shared secret. If set, every request must include header `X-Mcp-Key: <this value>`.
# MCP_ACCESS_KEY=
