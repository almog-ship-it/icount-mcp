# Used only by the local stdio shim (src/stdio.ts).
# The deployed Edge function ignores these — clients pass creds via headers.

ICOUNT_API_TOKEN=your-icount-api-token-here
ICOUNT_CID=your-icount-company-id-here
ICOUNT_DRY_RUN=0
