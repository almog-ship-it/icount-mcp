name = "icount-mcp"
main = "src/worker.ts"
compatibility_date = "2026-04-01"
compatibility_flags = ["nodejs_compat"]

# MCP_ACCESS_KEY (optional URL gate) is set as a secret, not a var:
#   wrangler secret put MCP_ACCESS_KEY
# Don't put secrets in this file — it's committed to git.
